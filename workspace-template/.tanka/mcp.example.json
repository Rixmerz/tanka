{
  "_comment": "Copy ONLY the servers you need into mcp.json. On a small model, more than ~3 servers or ~15 tools degrades tool selection. The resulting tool names are mcp__<server>__<tool>.",
  "mcpServers": {
    "gmail": {
      "type": "http",
      "url": "https://gmail.mcp.example/mcp"
    },
    "calendar": {
      "command": "npx",
      "args": ["-y", "@example/calendar-mcp"],
      "env": { "CALENDAR_TOKEN": "${CALENDAR_TOKEN}" }
    }
  }
}
